{
  "name": "ecommerce-kafka",
  "private": true,
  "workspaces": [
    "shared",
    "services/*",
    "frontend"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down -v"
  }
}